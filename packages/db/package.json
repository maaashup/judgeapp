{
  "name": "@judge/db",
  "private": true,
  "scripts": {
    "prisma:generate": "dotenv -e ../../.env -- prisma generate --config prisma.config.ts",
    "prisma:migrate": "dotenv -e ../../.env -- prisma migrate dev --config prisma.config.ts",
    "prisma:reset": "dotenv -e ../../.env -- prisma migrate reset",
    "prisma:studio": "dotenv -e ../../.env -- prisma studio --config prisma.config.ts"
  },
  "devDependencies": {
    "@prisma/client": "^7.4.2",
    "@prisma/config": "^7.4.2",
    "@types/node": "^25.0.3",
    "dotenv-cli": "^11.0.0",
    "prisma": "^7.4.2"
  }
}
