import 'package:flutter/material.dart';

class RulingsPage extends StatelessWidget {
  const RulingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Rulings')));
  }
}
